version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - wallpaper_data:/app/wallpaper_indexing

volumes:
  wallpaper_data:

